{
  "name": "soro-monorepo",
  "version": "1.0.0",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "node backend/dist/server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
